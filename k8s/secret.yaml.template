# This is a template file. Copy to secret.yaml and fill in base64-encoded values.
# To encode: echo -n "your-value" | base64

apiVersion: v1
kind: Secret
metadata:
  name: mock-api-studio-secret
  namespace: default
type: Opaque
data:
  # Database URL (base64 encoded)
  # Example: postgresql://user:password@postgres-service:5432/mockapi
  DATABASE_URL: <BASE64_ENCODED_DATABASE_URL>
  
  # JWT Secret (base64 encoded)
  JWT_SECRET: <BASE64_ENCODED_JWT_SECRET>

